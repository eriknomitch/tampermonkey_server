#= require utility
#= require_self
#= require bootstrap

trace "pre"

@GM =
  env: "<%= Rails.env %>"

GM.domain = switch
  when "development"
    "localhost:3000"
  when "production"
    "tapermonkey-server.herokuapp.com"

